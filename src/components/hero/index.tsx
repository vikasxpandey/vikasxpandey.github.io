import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

import './index.css';

const Hero = () => (
	<Box className='heroPaper'>
		<Typography variant='h1' className='title'>
			FULL
		</Typography>
		<Typography variant='h1' className='title'>
			STACK
		</Typography>
		<Typography variant='h1' className='title'>
			DEVELOPER
		</Typography>
	</Box>
);

export default Hero;
